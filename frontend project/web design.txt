repo scaffold — key files
1) root package.json (monorepo with web & mobile){
  "name": "eduplatform",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev:web": "cd apps/web && pnpm dev",
    "build:web": "cd apps/web && pnpm build",
    "start:web": "cd apps/web && pnpm start",
    "dev:mobile": "cd apps/mobile && pnpm start"
  },
  "devDependencies": {},
  "dependencies": {}
}